{
  "name": "wake-on-lan",
  "version": "1.0.0",
  "description": "Web Wake-on-LAN dengan manajemen device dan shutdown via SSH",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2",
    "ssh2": "^1.15.0"
  }
}
